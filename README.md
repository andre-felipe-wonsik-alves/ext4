# ext4

## Compilar e Executar

-- ```$ make all```: clean + build + run
--  ```$ make all img=<path_to_image>```: escolher uma imagem  
--> O arquivo ```resources/myext4image4k.img``` está definido como imagem padrão
--> A imagem padrão NÃO funciona, temos que criar uma nova pra testar
